# Markdown Renderer

## Purpose
Convert standard engine capability objects into valid GitHub-Flavored Markdown.

## Execution
Take the engine output object.
Apply headers (`#`, `##`).
Format arrays as list items (`- `).
Wrap code snippets in proper language blocks.
DO NOT alter the actual data or reasoning. Mute any generative tendencies.
