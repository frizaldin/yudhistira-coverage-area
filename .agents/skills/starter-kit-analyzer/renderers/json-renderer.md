# JSON Renderer

## Purpose
Strictly serialize capability outputs into structured JSON for `memory/` persistence.

## Execution
Take the engine output object.
Ensure output is 100% valid JSON.
No markdown wrappers (e.g., no ```json).
No conversational text.
