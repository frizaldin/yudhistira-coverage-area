# Dependency Engine

## Responsibility
Map layer access contracts (e.g., Controllers can access Services, but Models cannot access Controllers).
Enforce strict dependency rules within the repository.
